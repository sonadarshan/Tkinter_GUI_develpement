from tkinter import *
# This creates a window
root = Tk()
# To create a label which is nothing but a textview
Label1 = Label(root, text="Hello Extreme networks")
# We have to add the created label to the window root
Label1.pack()
# This loop keeps on running till the close button is pressed
root.mainloop()